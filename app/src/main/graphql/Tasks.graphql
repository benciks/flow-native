query Tasks($filter: TaskFilter) {
    tasks(filter: $filter) {
        id
        uuid
        description
        modified
        entry
        urgency
        status
        due
        priority
        project
        tags
    }
}